#!/usr/bin/env bash
# Boots the full dev stack (Rails API + Vite + Sidekiq) via Procfile.dev.
set -e

if ! gem list foreman -i --silent >/dev/null 2>&1; then
  echo "Installing foreman…"
  gem install foreman
fi

exec foreman start -f Procfile.dev "$@"
