<%= t('mailers.common.hello') %><%= @client.name.present? ? ", #{@client.name}" : "" %>!

<%= t('mailers.invoice.canceled.text_body', reference: @invoice.external_reference, amount: email_brl(@invoice.amount_cents), workspace: @workspace.name) %>

<%= t('mailers.invoice.canceled.text_note', workspace: @workspace.name) %>
