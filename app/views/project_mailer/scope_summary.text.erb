Olá<%= @client&.name.present? ? ", #{@client.name}" : "" %>!

Segue o panorama do conteúdo planejado para <%= @project.name %>.

<% if @tickets.any? %>
<% @tickets.each do |ticket| %>
- <%= ticket.display_title %>
  <%= ticket.creative_types_list.map { |t| creative_type_label(t) }.join(" · ").presence || "—" %> · <%= status_label(ticket.status) %> · <%= ticket.scheduled_at ? email_date(ticket.scheduled_at) : "Data a definir" %>
<% end %>
<% else %>
Nenhum ticket criado neste projeto ainda.
<% end %>
Datas sujeitas a ajustes durante a produção.
