<%= t('mailers.common.hello') %><%= @owner&.display_name.present? ? ", #{@owner.display_name}" : "" %>!

<%= t('mailers.subscription.canceled.text_body', plan: @plan_label, workspace: @workspace.name) %>
<%= t('mailers.subscription.canceled.text_body_note') %>

<%= t('mailers.subscription.canceled.text_body2') %>
<%= @billing_url %>
