<%= @recipient.display_name.present? ? "#{t('mailers.common.hello')}, #{@recipient.display_name}!" : "#{t('mailers.common.hello')}!" %>

<%= t('mailers.note.mention.text_body', author: @author_name, title: @ticket.display_title) %>

<%= @note.plain_body %>

<%= t('mailers.common.open_ticket') %>: <%= @ticket_url %>

—
<%= t('mailers.note.mention.text_note') %>
