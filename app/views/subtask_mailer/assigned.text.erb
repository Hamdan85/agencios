<%= t('mailers.common.hello') %>, <%= @assignee.display_name %>!

<% if @actor_name %><%= t('mailers.subtask.assigned.text_assigned_by', name: @actor_name) %><% else %><%= t('mailers.subtask.assigned.text_assigned_generic') %><% end %> <%= t('mailers.subtask.assigned.text_body_suffix', title: @ticket.display_title) %>

✓ <%= @subtask.title %>
<% if @subtask.due_date %><%= t('mailers.subtask.assigned.text_due') %> <%= email_date(@subtask.due_date) %>
<% end %>
<%= t('mailers.subtask.assigned.text_view') %> <%= @ticket_url %>
