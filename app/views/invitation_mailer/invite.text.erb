<% if @inviter_name %><%= t('mailers.invitation.invite.text_invited_by', name: @inviter_name) %><% else %><%= t('mailers.invitation.invite.invited_generic') %><% end %> <%= t('mailers.invitation.invite.text_body', workspace: @workspace.name, role: @role_label) %>

<%= t('mailers.invitation.invite.text_accept') %> <%= @link %>

<%= t('mailers.invitation.invite.text_note') %>
