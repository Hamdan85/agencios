<%= t('mailers.report.deck.text_heading', project: @project.name) %>

<%= t('mailers.common.hello') %><%= @client&.name.present? ? ", #{@client.name}" : "" %>. <%= t('mailers.report.deck.text_body', project: @project.name) %>

<%= t('mailers.report.deck.text_open') %> <%= @url %>

<%= t('mailers.report.deck.note') %>
