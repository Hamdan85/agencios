<%= t('mailers.common.hello') %><%= @client&.name.present? ? ", #{@client.name}" : "" %>!

<%= t('mailers.approval.review.text_body', project: @project.name) %>

<%= @url %>

<%= t('mailers.approval.review.note') %>
